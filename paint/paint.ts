import type { Node } from "../dom/node";
import type { DisplayList } from "./display-list";

export function paint(_dom: Node): DisplayList {
  // Painting will be implemented in the next commit.
  return [];
}
